import React from 'react';

const ErrorMessiges = () => {
    return (
        <div className='h-screen flex items-center justify-center'>
            <button className="btn loading">loading</button>
        </div>
    );
};

export default ErrorMessiges;